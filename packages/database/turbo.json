{
  "$schema": "https://turbo.build/schema.json",
  "extends": [
    "//"
  ],
  "tasks": {
    "build": {
      "dependsOn": [
        "prisma:generate"
      ],
      "outputs": [
        "lib/**"
      ]
    }
  }
}
